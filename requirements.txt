# Claude API — Dream Cycle classification/extraction (not embeddings; those are local)
anthropic>=0.50.0

# MCP server
mcp>=1.9.0

# Storage substrate: SQLite + sqlite-vec now live inside the Rust
# core. The `sinam_core` wheel is built from the sinam-core repo
# (crates/sinam-core-py, `maturin build --release`) and installed by hand —
# it is not on PyPI yet. It must be the ONLY SQLite library in the process
# (a second one, e.g. apsw, cannot see its locks and corrupts the database).
# sinam_core (local wheel)

# Local embeddings + date resolution now run inside the Rust core (the
# sinam_core wheel). The model files live in ~/.synapse/models/… — use
# `pip install fastembed` once + tools/gen_reference_vectors.py in the
# sinam-core repo, or copy them from another install.

# .env loading
python-dotenv>=1.0.0

# Tests
pytest>=8.0.0
httpx>=0.27.0          # FastAPI TestClient

# Future: Dream Cycle REST API
fastapi>=0.115.0
uvicorn[standard]>=0.34.0

# LAN auto-discovery (mDNS) — phones find the brain Mac without typing an IP
zeroconf>=0.131.0

# Mac<->Mac sync transport: the pull loop in api/sync_peers.py.
# Was missing from a fresh install (prod venv had it transitively) — a clean
# `pip install -r requirements.txt` could not start the API.
requests>=2.31.0

# TESTS ONLY since the living map moved into the core. The map's clustering and
# layout used to run here (networkx Louvain + ForceAtlas2); both now live in
# `sinam-core` and the endpoint serves what the core computes. What is left is
# the karate club graph, the reference benchmark our Louvain is pinned against
# in test_api.py. The runtime, and the .dmg, no longer need it.
networkx>=3.2
